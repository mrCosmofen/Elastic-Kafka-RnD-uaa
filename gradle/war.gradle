apply plugin: "war"

bootWar {
    mainClassName = "com.epam.elastic_kafka_rnd.uaa.UaaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
